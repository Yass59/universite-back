package fr.roudane.pizzeria.controller;

import org.springframework.ui.Model;

import fr.roudane.pizzeria.domain.PizzeriaBean;

public interface IPizzeriaController {

	public String getPizza(Model model);
	
	public String InfoPizza (Model model, Integer id);
	
	public String supprimerPizza (Model model, Integer id);
	
	public String ajouterPizza (Model model, PizzeriaBean piza);
	
	
}
